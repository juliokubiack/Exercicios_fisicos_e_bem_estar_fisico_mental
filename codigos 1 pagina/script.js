let objetivo=0;
let dias=0;
let entrar=prompt("você deseja continuar?").toUpperCase();
if(entrar=="SIM"){
    dias=+prompt("quantos dias você pode dias ativos na semana vocÊ pode estar contribuindo para o proceguimento de nossos objetivos ")
    do{
        objetivo=+prompt("qual é o seu objetivo dentro de nosso...").toUpperCase()

    }
    while(dias<0 || objetivo != "EMAGRECIMENTO" || objetivo != "GANHO MASSA")
}
else{
    Sair...
}